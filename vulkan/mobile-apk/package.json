{
  "name": "vulkan-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that packages the Vulkan mobile UI as an Android APK",
  "scripts": {
    "setup": "npm install && npx cap add android",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
